# 系统完善路线图（ROADMAP）

> 适用项目：空天海网络可视化实时交互仿真演示系统
> 日期：2026-07-29
> 状态：阶段 1、2、3、4、5、6 已实施（于 v3 工作副本 `dayilixiang-v3`）
> 维护：李思达
>
> **进度**：✅ 阶段 1（协议与指标地基）｜✅ 阶段 2（DES 核心）｜✅ 阶段 3（多域流）｜✅ 阶段 4（解耦平滑）｜✅ 阶段 5（包级可视化）｜✅ 阶段 6（校验加固）
>
> **v2 / v3 分线**：`dayilixiang`（桌面原文件夹）保留为 v2 稳定版；`dayilixiang-v3` 为 v3 开发线，本路线图及后续开发均在此进行。

---

## 0. 最高目标

**在完成多域节点（卫星 + 无人机 + 船舶 + 地面站）扩展的基础上，实现包级仿真（packet-level simulation）与流畅的实时可视化。**

当前系统的指标（带宽利用率、时延、丢包）是由正弦波叠加噪声"拍脑袋"生成的，没有真实的数据包在流动。本路线图的目标是让这些指标**从真实数据包的转发过程中涌现**，并把这条"仿真 → 传输 → 渲染"的链路做到解耦、平滑、可验证。

两根支柱：

- **支柱 A — 包级仿真**：指标从假数据变成真实数据包跑出来的结果。
- **支柱 B — 流畅实时可视化**：仿真、传输、渲染三层解耦，前端 60fps 平滑运动。

---

## 1. 关键技术决策

### 1.1 包级仿真核心：自研 Python 离散事件仿真器（DES），不接 ns-3

Hypatia 原生框架基于 ns-3（C++）做包级仿真，但存在以下问题：

- ns-3 是重型 C++ 框架，编译环境繁琐，学习曲线陡；
- ns-3 天然偏向**离线 trace 回放**，与"实时推送 + 交互控制"耦合困难；
- 多域（UAV/船舶）扩展需要改动其底层，成本高、不可控。

**结论：自研轻量 Python DES。** 用 `heapq` 事件队列 + 存储转发 + 每端口 FIFO 队列，约 800 行即可产出真正的包级指标（端到端时延、抖动、丢包、链路利用率、队列深度），并能无缝接入现有 `demo_sim_core → realtime_backend → 前端` 管线，完全自主可控、便于讲解与扩展。ns-3 保留为未来"科研级对标"的可选分支，不在本路线内。

### 1.2 三层解耦

```
[DES 仿真进程]  →  [状态快照 5–10Hz]  →  [后端 WS 转发]  →  [前端 60fps 插值渲染]
   可加速运行          固定节拍聚合          透明转发           SampledPosition 平滑
```

仿真频率、推送频率、渲染频率三者彻底解耦：DES 可以跑得比实时快，推送按固定墙钟节拍，前端用插值把离散位置流平滑成连续运动。

### 1.3 后端定位不变

`realtime_backend` 维持**纯 JSON 透明转发层**的定位，包级仿真全部落在 `demo_sim_core`（或其拆分出的仿真引擎模块），前端负责渲染与交互。后端无需为包级仿真做结构性改动。

---

## 2. 六阶段路线

### 阶段 1 — 协议与指标地基

**目标**：为包级遥测定义数据形状，前后端先对齐，避免核心写完后反复改协议。

**内容**：

- 在 protocol v2 基础上扩展（或升级 v3），新增包级遥测字段：
  - 每条链路：`tx_bps` / `capacity_bps` / `queue_depth` / `propagation_ms`；
  - 每个节点/流：`pkts_sent` / `pkts_recv` / `drops` / `e2e_latency_ms` / `jitter_ms`。
- 保留现有字段，向后兼容。
- 前端详情面板预留这些字段的展示位（先用占位数据）。

**产出 / 验收**：协议字段文档（更新 `docs/protocol-v2-multidomain.md` 或新增 v3 文档）；前端详情面板能渲染新字段。

---

### 阶段 2 — 包级仿真核心（心脏）

**目标**：用真实数据包替代假指标。

**内容**：

- **事件驱动引擎**：`heapq` 事件队列，事件 = `(time, type, packet)`；仿真时钟支持加速 / 暂停 / 跳转。
- **流量模型**：每节点 CBR / Poisson 发生器（UAV→地面站回传、船→岸基、星间转发负载）。
- **动态拓扑 + 路由**：基于现有 K-最近可见链路构建时变图；拓扑变化或周期触发 Dijkstra 重算转发路径。
- **转发引擎**：每输出端口 FIFO 队列，存储转发（发送时延 = 包长 / 带宽）；传播时延 = 斜距 / 光速（真实几何）。
- **丢包模型**：队列溢出（有限缓存）+ 链路误码；接续现有 scenario 概念，将"理想/商用/天气/切换/极端"映射到负载强度与误码率。
- **指标采集**：逐包打时间戳 → 端到端时延 / 抖动；逐链路字节计数 → 利用率 / 队列深度。

**产出 / 验收**：先在**仅卫星域**跑通；验收标准——端到端时延 ≈ 几何传播时延 + 排队时延，可与理论值对账。

> **✅ 已完成（2026-07-29）**：新增 `packet_sim.py`（`PacketEngine`，约 500 行）——`heapq` 事件队列（GEN/TX/ARR 三类事件）、每端口 FIFO + 单发送机存储转发、反向 Dijkstra 逐跳路由（周期 + 拓扑变更刷新）、Poisson 流量发生、队列溢出丢包、传播时延取真实几何斜距。`demo_sim_core` 每 tick 调用 `sync_topology / sync_flows / advance / snapshot`，链路利用率/时延/丢包/吞吐/队列深度与节点计数/端到端时延/抖动**全部来自真实数据包**，正弦波假指标已移除。
>
> 验收对账：轻载 3 跳链 e2e = 21.037ms，与理论值（21ms 传播 + 0.037ms 发送）精确吻合；拥塞场景正确涌现排队（e2e 升至秒级）、队列溢出丢包与链路丢包率。全管线离线测试（72 星 + 8 机 + 10 船 + 15 站，30s 仿真）：送达 ~105 万包、零丢包、聚合吞吐 ~1 Gbps、平均 e2e 13.4ms，27 tick/s（5Hz 实时目标的 5 倍余量）。seek/暂停/恢复的时钟不连续正确触发 flush。

---

### 阶段 3 — 多域包级数据流（UAV + 船舶）

**目标**：数据包真正穿越 `SUL→ISL→GSL` / `SSL→ISL→GSL` 全链路。

**内容**：

- UAV 编队内部流量 + 对地回传；船舶对岸流量。
- 切换时的在途包处理：重路由 or 丢弃，做成**真实的切换丢包**现象。
- 分域 QoS 优先级（如 UAV 测控遥测优先于批量数据）。

**产出 / 验收**：每条上行链路都有真实吞吐 / 时延 / 丢包；链路切换时可观测到丢包尖峰。

> **实施记录（2026-07-29）**：
> - **真实切换丢包**：`sync_topology` 改为差异化更新——存活链路保留 Link/Port（队列、在途包、字节计数不受无关链路来去影响）；移除的链路被排空，其队列与在途包逐包计为**切换丢包**（per-node / per-link / 全局 `pkts_handover_dropped` 计数并同步扣减 `in_flight`，顺带修复了旧版整表重建导致的在途包泄漏）。新增链路才建新端口。
> - **分域 QoS**：`Packet.prio`（0=高优先，1=尽力）；每端口多级优先队列 + 严格优先调度（`pop_next` 先取最高优先非空队列）；缓冲满时**低优先先丢**——高优先包到达会挤出一个最低优先在队包，从而在拥塞下保护优先流量。新增 per-priority 生成/送达/丢包计数，`snapshot.summary.qos` 暴露。
> - **接入**：`demo_sim_core._des_step` 给每条流打优先级（UAV=高优先测控遥测，船=尽力批量数据），经 `sync_flows(flow_prio=...)` 下发；`metrics_summary` 新增 `pkts_handover_dropped` 与 `qos`；前端统计面板新增「切换丢包」「QoS丢包 高优先/尽力」两行（cache-bust v=3d0）。
> - **验收对账**：`test_phase3.py`——切换测试在拥塞上行链路移除瞬间丢包尖峰 = 201（200 在队 + 1 在途），与 `pkts_handover_dropped` 精确一致，随后流量经新链路恢复；QoS 测试在共享瓶颈下高优先丢包 0.000 / e2e 35ms，尽力流丢包 0.605 / e2e 1065ms，优先级区分显著。Phase 2 单元测试与全管线离线测试均回归通过（~105 万包送达、e2e 13.4ms、~1 Gbps）。120s 实时仿真观测到 8 次 SUL + 6 次 SSL 切换；轻载下切换瞬间队列为空故丢包≈0（符合物理现实），负载升高时尖峰即现。

---

### 阶段 4 — 实时解耦与渲染平滑

**目标**：仿真再重，画面也稳。

**内容**：

- DES 独立进程 / 线程运行，按固定节拍（5–10Hz）聚合快照推送。
- **前端位置插值**：用 Cesium `SampledPositionProperty` 或渲染循环手动 lerp，把 5Hz 位置流平滑成 60fps 连续运动（"流畅"最直接的来源）。
- **时钟管理**：倍速 / 暂停 / seek 统一作用于 DES 时钟，前后端状态一致。

**产出 / 验收**：10x 倍速下帧率不掉、位置不跳变；CPU 有预算、热点可测量。

> 注：本阶段的"前端插值"不依赖包级核心，可**提前作为快速收益**与阶段 2 并行实施。

> **实施记录（2026-07-29）**：
> - **前端位置插值（核心交付）**：`cesium-manager.js` 中节点位置从「每次 state_update 直接 `entity.position = 常量`（5Hz 瞬移）」改为**墙钟时间插值**——每个节点维护一段插值状态 `{prev, target, t0, dur}`，`entity.position` 绑定一个 `CallbackProperty`，在**每个渲染帧**按 `alpha = clamp((now - t0)/dur, 0, 1)` 在上一采样点与最新采样点之间做 `Cartesian3.lerp`（复用 scratch，避免逐帧分配）。新采样到达时以「当前显示位置」为新段起点，段长按实测到达间隔自适应（clamp 40–1000ms），从而把 5Hz 位置流平滑成显示器刷新率（≈60fps+）的连续运动，且对网络抖动鲁棒。链路端点本就用 `CallbackProperty` 逐帧读取节点位置，自动跟随平滑。`clearAll` 同步清空插值表。
> - **为何不动后端**：`demo_sim_core` 已是**独立进程**、以固定 5Hz 墙钟节拍 tick（`asyncio.sleep(update_interval - elapsed)`，与倍速无关），满足"DES 独立进程 + 定节拍推送"；倍速只改变每 tick 的 `sim_time` 增量（位移变大），推送频率不变，故插值间隔恒定、运动依旧平滑。
> - **时钟管理**：仿真时钟由后端 `sim_time` 唯一拥有，前端仅显示 `payload.timestamp` 并在墙钟域插值位置；暂停时后端持续推送冻结位置（节点 alpha→1 自然停稳，无漂移）；seek 触发 DES flush，前端以 ~200ms 滑移过渡。Cesium 内置 clock 不再驱动渲染（`CallbackProperty` 忽略时间参数），倍速对其的设置变为纯展示，前后端状态一致。
> - **验收对账**：浏览器实测 1x 与 10x 下帧率均稳定 **120 FPS 不掉**；10x 时仿真时间数秒内从 24s 推进到 270s，位置经插值连续运动无瞬移/跳变；指标照常涌现（送达 9.45M、e2e 9.3ms、吞吐 842 Mbps）。cache-bust v=3e0。

---

### 阶段 5 — 包级数据可视化 ✅

**目标**：让"包"被看见。

**内容**：

- ✅ 链路按真实吞吐 / 队列深度着色（复用现有颜色映射，换成真数据）。
- ✅ 新增时序图表面板：吞吐 / 时延 / 丢包随仿真时间变化的曲线。
- ✅ 真实路由路径高亮（阶段 2 已实现，由实际转发路径 `payload.routing` 驱动）。
- ✅ 可选增强：沿链路动画展示数据包流动（已完成，2026-07-29）。

**产出 / 验收**：点击任意链路 / 节点，看到的是随时间演化的真实曲线，而非静态数字。

> **实施记录（2026-07-29）**：
> - **链路着色（真数据驱动）**：`cesium-manager.js` 新增 `_linkMetricValue(props, mode)`，把每条链路的实时指标归一化到 0..1（`bandwidth`=利用率、`queue`=队列深度/容量、`latency`=时延/50ms、`loss_rate`=丢包率×100），再经既有 `_getGradientColor` 绿→黄→红渐变着色；量化到 0.05 档避免 5Hz 逐 tick 重建材质。`addOrUpdateLink` 创建/更新分支、`setMetricsMode`、`_restoreEntityStyle`、`clearRouteHighlights` 统一改传完整链路属性快照，使着色随真实吞吐/队列逐 tick 演化。图层面板新增「链路着色」下拉（按类型/吞吐利用率/队列深度/时延/丢包率）切换 `metricsMode`。
> - **时序图表面板**：新增零依赖 `js/chart.js`（`TimeSeriesChart`，Canvas 2D，自动量程 + 滚动点缓冲 + ~10fps 限频重绘 + 仿真重启/回拉自动清空）。`ui-controller.js` 实例化吞吐(Mbps)/端到端时延(ms)/丢包(pkt/s) 三条曲线，`pushCharts(timestamp, summary)` 以 ~2Hz 从 `metrics_summary` 取数——丢包用累计 `pkts_dropped` 的差分除以仿真时间差得瞬时速率；`app.js` 在每次 state_update 喂数、simulation_init 时 `resetCharts()` 清空。左下新增可折叠「时序指标」玻璃面板（index.html）。
> - **验收对账**：浏览器实测时序面板实时涌现（吞吐 2.06 Mbps、时延 79ms、丢包 1 pkt/s 曲线随时间推进）；着色下拉切换后链路按对应指标渐变着色。后端零改动。cache-bust v=3f0。
> - **沿链路包流动动画（可选增强，2026-07-29）**：新增零依赖 `js/packet-flow.js`（`PacketFlowManager`）——为每条活跃链路生成 1–4 个白色「数据包」点实体（数量随利用率提升），沿链路从 source 端向 target 端循环流动。包位置用 `CallbackProperty` 在**每个渲染帧**对两端节点的实时插值位置（复用阶段 4 的 `_sampleNode`）做 `Cartesian3.lerp`，按墙钟时间推进相位，故与仿真倍速/暂停无关、任意时刻都平滑流动；流速随利用率 1200ms→500ms 加快。全局上限 250 个包（超额按比例缩减），链路移除/`clearAll` 时同步清理；拾取守卫使点击包等同于点空白（不干扰链路/节点选择）。**刻意不触碰链路材质**，与既有颜色缓存/指标渐变逻辑零耦合。图层面板新增「动画 → 包流动动画」开关（默认开），`cesium-manager.setPacketFlow()` 驱动。**验收对账**：注入临时脚本读 `packetFlow._all.length` 得 **n=211 / byLink=211 / enabled=true**，与 211 条活跃链路精确一致（轻载每链 1 包），帧率稳定 120 FPS。后端零改动。cache-bust v=3g0。

---

### 阶段 6 — 校验与加固 ✅

**目标**：让系统经得起推敲。

**内容**：

- 仿真器对账：时延 / 吞吐 vs 理论值。
- 拥塞与长时运行压测；断线重连健壮性。
- 文档与 README 更新。

**产出 / 验收**：性能 / 正确性报告 + 可复现的压测脚本。

> **实施记录（2026-07-29）**：
> - **对账脚本 `test_phase6.py`（测试 5–11，固定种子可复现）**：
>   - 包守恒不变量：`生成 = 送达 + 丢弃 + 在途` 在切换+拥塞混流、1 小时长时、背压过载、全管线 300 s 四个场景**精确**成立（分优先级残差之和 = 在途数）；
>   - 吞吐对账：轻载每跳 tx_bps 误差 ≤4.2%（Poisson 噪声），GSL 利用率 0.115 vs 理论 0.120；
>   - 逐跳时延分解：各链路时延 = 传播 + 发送，误差 ≤0.075 ms，e2e = Σ逐跳停留**精确可加**；
>   - M/D/1 排队论对账：ρ=0.8 瓶颈平均等待理论 12 ms，实测 e2e 23.60 vs 理论 24.00 ms（误差 1.7%），排队时延从仿真中涌现而非拟合；
>   - 1 小时长时压测：18,000 tick、链路每 10 s 切换、ρ≈0.96，1,160 ticks/s 无退化（首末千 tick 0.88 s / 0.85 s），事件堆/链路字典无泄漏，切换丢包 522；
>   - 背压安全阀：单慢链路过载下在途峰值被精确钳制在 `max_in_flight`=100；
>   - 全管线长时：真实 DemoSimCore 300 s 仿真送达 10,505,465 包、零丢包、19 ticks/s（约 3.8× 实时余量）、守恒精确。
> - **断线重连加固**：修复 `demo_sim_core` 运行中连接断开即退出的问题——改为 2 s 退避自动重连（每次故障最多 30 次）、重连重发 init，时间间隔 > `MAX_DES_STEP` 自动 flush DES；`test_reconnect.py`（R1–R5）以真实子进程验证：杀核心→后端存活+命令返回 `no_core_connected`；重启核心→在线客户端收新 init；新客户端→后端立即重放 init；**杀后端→核心存活并自动重连**（回归项）。前端指数退避重连 + 后端 init 重放 + 核心自愈，三层各自具备断线恢复能力。
> - **文档**：新增 `docs/phase6-validation.md` 性能/正确性报告；README 补充测试套件说明；清理 v2 遗留失效测试 `test_constellation.py`（引用已移除的星座切换 API）。
> - **验收**：测试 1–11 + R1–R5 全部通过（`test_packet_sim.py` / `test_phase3.py` / `test_integration_offline.py` 回归通过）。

---

## 3. 依赖关系与执行顺序

```
阶段1(协议地基) → 阶段2(DES核心) → 阶段3(多域流) → 阶段5(包级可视化) → 阶段6(校验加固)
                       └── 阶段4(解耦平滑) 并行 ──┘
```

- 阶段 1 是地基，阶段 2 是核心（工作量最大、价值最高）。
- 阶段 3 依赖阶段 2 的转发引擎。
- 阶段 4 的前端插值部分可提前并行；其"DES 进程化 + 时钟管理"部分在阶段 2/3 之后收口。
- 阶段 5 依赖阶段 2/3 产出的真数据。
- 阶段 6 收尾。

**建议执行顺序**：`1 → 2 →（4 的插值并行）→ 3 → 4 剩余 → 5 → 6`

---

## 4. 当前状态（基线）

- **六阶段路线全部完成**（协议地基 → DES 核心 → 多域流 → 解耦平滑 → 包级可视化 → 校验加固）。
- 三层架构已联调：`demo_sim_core`（圆轨道传播 + **包级 DES 指标**，5Hz 推送）→ `realtime_backend`（FastAPI + WS 透明转发）→ 前端（CesiumJS 1.141，v3 极简 UI + 墙钟插值 + 链路指标着色 + 时序图表 + 包流动动画）。
- 多域节点已就位：72 卫星 + 8 无人机 + 10 船舶 + 15 地面站。
- SUL/SSL 上行链路采用「K 个最近可见卫星」模型（仰角掩码 5° + 迟滞 + 兜底），每个 UAV/船舶任意时刻至少 1 条上行链路。
- **指标已包级化**：链路利用率/时延/丢包/吞吐/队列深度、节点收发/转发/丢包计数、端到端时延/抖动全部由自研 DES（`packet_sim.py`）中真实数据包涌现，正弦波假指标已移除；切换丢包与分域 QoS（UAV 高优先 / 船舶尽力）真实可观测。
- **已校验加固**：时延/吞吐与解析理论对账吻合（M/D/1 误差 1.7%、逐跳分解误差 <0.1 ms），包守恒不变量精确成立；1 小时仿真与 10.5M 包全管线运行无退化无泄漏；前端/后端/核心三层均具备断线自愈能力。详见 `docs/phase6-validation.md`，复现脚本见 `hypatia-master/satviz/test_*.py`。

---

## 5. 后续发展方向（专业化路线）

> 六阶段工程路线已收官。以下为专业化候选路线，按教育 / 学术 / 商业三个方向与共享技术地基组织（2026-07-29 规划）。核心判断：系统最值钱的资产是**经过理论对账的包级仿真器**，三个方向都以此为硬通货。

### 5.1 共享技术地基（三个方向的共同前置）

- **规模扩展至千星级**：Walker-δ 星座生成、路由重算与链路管理优化（当前 72 星；千星级支持本身即值得发表的技术贡献）。
- **轨道真实性**：SGP4 + 真实 TLE（Celestrak）替代圆轨道（仓库内 satgenpy 可直接用）；GSL 雨衰 / 多普勒等信道效应。
- **数据接入模式**：三层架构（WS 中继 + Cesium）允许把仿真核心替换为真实数据源（TLE + 遥测），从「仿真器」演进为「数字孪生运控可视化平台」。

### 5.2 教育方向（当前性价比最高）

- **产品形态**：卫星网络虚拟仿真实验平台——能实时看见数据包流动、队列堆积、切换丢包的网络实验环境（ns-3 / mininet 均无实时可视化）。
- **实验课程**（Phase 6 对账脚本即现成答案）：① 端到端时延 vs 理论值对账；② 制造拥塞观察 M/D/1 排队曲线；③ 切换丢包尖峰观测；④ QoS 优先级对比。每个实验自带理论答案。
- **参数可调交互**：学生先设置带宽 / 速率 / 队列长度并预测结果，再运行验证。
- **可争取抓手**：国家级 / 省级虚拟仿真实验教学项目；挑战杯、中国国际大学生创新大赛（「硬科技 + 教学平台」叙事）；《实验技术与管理》《实验科学与技术》等实验教改期刊（对本科生友好）。
- **在地优势**：哈工大学生卫星（紫丁香系列）与航天相关实验室——与卫星地面段 / 数传教学挂钩可显著提升项目分量。

### 5.3 学术方向

- **关键动作**：把 `packet_sim.py` 包装为强化学习环境（gym 接口；`sync_topology / sync_flows / advance / snapshot` 天然适配）。LEO 路由 DRL 研究普遍缺少快、开源、可复现的仿真环境（ns-3 太慢、商业闭源）；本 DES 实测 1160 ticks/s，性能即卖点。
- **第一篇论文**：一个路由对比实验（最短传播时延 vs 负载感知路由的时延 / 丢包 / 吞吐）→ IEEE ICC / GLOBECOM 卫星通信 workshop 或 AIAA ICSSS。
- **第二条路径**：仿真器本身作为成果 → ACM TOMACS（专收仿真软件）或《系统仿真学报》。
- **可信度门槛**：与公开测量数据对账（Starlink 时延测量数据集）、千星级规模验证。

### 5.4 商业方向（最难，侧翼切入）

- 正面竞争商业仿真软件（STK、ns-3 商业支持、院所自研工具）不现实。
- **侧翼一：展厅 / 科普 / 路演可视化市场**——大屏实时 3D 星座演示（卫星公司路演、国防科普馆）；卖可视化层，不卖仿真精度。
- **侧翼二：数字孪生运控平台**——见 5.1 数据接入模式，商业叙事价值更高。
- **现实路径**：比赛拿奖 → 高校孵化器，或带入老师课题组 / 公司；文档、支持、渠道远超单人开发者能力，不宜直接启动。

### 5.5 建议优先级

1. **近期（本学期，投入小、回报确定）**：打包 2–3 个完整实验 + 对账报告 → 投比赛或实验类期刊；主动对接航天方向老师。
2. **中期**：gym 环境封装 + 路由对比实验 → workshop 论文。
3. **长期**：千星级规模扩展 + 真实数据接入；学术 / 教育名气建立后再考虑商业化。

---

## 6. 5.1 实现规划：阶段 7–9（共享技术地基）

> 依赖顺序 `7 → 8 → 9`：规模先稳定，再换真实轨道，最后接真实数据。阶段 8 的星历抽象与阶段 7 可部分并行。工作量按单人开发估计：7 约 2–3 周，8 约 2 周，9 约 1 周。
>
> 代码地基（现状）：`Satellite` 为单层圆轨道；`create_constellation` 已是 Walker 风格（奇偶轨道面错相）；ISL 在初始化时静态计算（`compute_links`）；GSL 用距离迟滞（2000/2200 km），SUL/SSL 用「K 个最近可见星」（仰角掩码 5°）；`packet_sim` 已有拓扑 dirty 标志触发路由重算、`link_error_rate` 接口与切换丢包机制。

### 阶段 7 — 千星级规模

**目标**：1000+ 卫星 / ~2500 链路下，核心 ≥20 ticks/s、前端 ≥30 FPS（1080p），包守恒仍精确。

**内容**：

- **多壳层 Walker-δ 生成器**：`create_constellation` 扩展为壳层列表（高度 / 倾角 / 轨道面数 × 每面星数 × 相位因子 F），ID 改为 `Sat-{shell}-{plane}-{idx}`；CLI 预设 `--scale 72|440|1584`。
- **结构化 ISL 邻接**：面内 (i±1) + 面间同纬度邻接（极区缝处理），O(N) 生成替代 O(N²) 两两距离；周期性重算（如 30 s）+ 差分更新，链路移除自动走 `packet_sim` 既有切换丢包路径。
- **profiling 与热点优化**：`cProfile` 定位；GSL/SUL/SSL 可见性重算降频至 1 Hz（tick 间保持）；路由重算仅在 dirty 时触发（已有标志）并放宽间隔。
- **协议瘦身**：`state_update` 的 links 改为增量（仅变化链路）或分级（全量位置 + 仅承载链路），协议加版本字段，前端双模兼容。
- **前端 LOD**：仅渲染承载链路 / 按相机距离过滤，节点点聚合，实体池复用（避免每帧重建）。

**产出 / 验收**：1584 星预设稳定运行 600 s，核心 ≥20 ticks/s、前端 ≥30 FPS；稳态单帧 WS 负载 <100 KB；`test_phase6` 全绿（守恒精确）；新增千星级压测脚本。

### 阶段 8 — 轨道真实性

**目标**：SGP4 + 真实 TLE 替代圆轨道，GSL 增加链路预算（雨衰 + 多普勒），与公开数据可验证。

**内容**：

- **星历提供者抽象**：`CircularProvider`（现 `Satellite` 逻辑原样封装）与 `SGP4Provider`（纯 Python `sgp4` 包，无需编译仓库内 satgenpy）；`Satellite.get_position` 委托提供者；CLI `--ephemeris circular|sgp4 --tle <file|url>`。
- **TLE 数据**：随仓库附带 Starlink 子集样本（~100 星）；可选从 Celestrak 拉取最新（离线自动回退样本）。
- **GSL 链路预算**：自由空间路损 + ITU-R P.618 简化雨衰（按地面站雨强）+ 多普勒 → 链路余量 → 动态调制 `packet_sim` 的链路容量与 `link_error_rate`（接口已存在）。
- **对账验证**：SGP4 位置 vs TLE 参考轨；GSL 丢包率随仰角 / 雨强的可观测变化；圆轨道模式保留为默认回归基线。

**产出 / 验收**：SGP4 位置与参考误差 <1 km；`test_phase6` 在 SGP4 模式下回归通过；新增 `docs/orbital-validation.md`。

### 阶段 9 — 数据接入 / 数字孪生

**目标**：真实 TLE 驱动的实时模式（墙钟传播），协议不变、前端零改动。

**内容**：

- **孪生核心**：新增 `twin_core.py`（或 `demo_sim_core --mode twin`）：位置 = SGP4(墙钟 × 倍速)，ISL/GSL 由真实几何计算，DES 可选挂载（在真实拓扑上跑包级仿真）。
- **数据源接口**：抽象 TLE 来源（本地文件 / Celestrak 定时轮询 / 外部推送）；协议预留可选遥测叠加字段（向后兼容）。
- **前端模式标识**：「仿真 / 实时」徽章（仅 UI 提示，不改协议）。

**产出 / 验收**：真实 Starlink TLE 子集按墙钟实时运动渲染；真实拓扑上 DES 守恒精确；前端协议零改动（仅新增徽章）。
